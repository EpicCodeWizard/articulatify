# Articulatify
Generate bulk course articulations spreadsheets using Assist.org. Built using Python and Gradio. To install the necessary packages and run the server:
```py
pip install openpyxl requests gradio pytz titlecase
gradio main.py
```
Output index.html also provided.
